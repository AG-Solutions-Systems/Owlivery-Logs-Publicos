{
  "name": "dedicated_api",
  "version": "1.0.0",
  "description": "dedicated api for ag solutions",
  "main": "app.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "set NODE_ENV=production&& fastify start -p 80 -l info -P app.js",
    "dev": "set NODE_ENV=development&& fastify start -p 8080 -w -l info -P app.js",
    "local": "set NODE_ENV=local&& fastify start -p 8081 -w -l info -P app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/autoload": "^5.0.0",
    "@fastify/cors": "^8.2.1",
    "@fastify/multipart": "^7.5.0",
    "@fastify/rate-limit": "^7.6.0",
    "@fastify/sensible": "^5.0.0",
    "@fastify/static": "^6.9.0",
    "axios": "^1.3.3",
    "discord.js": "^14.8.0",
    "fastify": "^4.0.0",
    "fastify-cli": "^5.7.1",
    "fastify-plugin": "^4.0.0"
  },
  "devDependencies": {
    "tap": "^16.1.0"
  }
}
